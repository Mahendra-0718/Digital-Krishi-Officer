/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Ensure we can use assets from public
}

module.exports = nextConfig
// Configuration updated to force cache refresh
